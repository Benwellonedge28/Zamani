[package]
name = "zenith"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
authors = ["Samuel Mukandara <samuelmukandara@gmail.com>"]
description = "Zenith: The Omniversal AGI Language & Operating Intelligence"
repository = "https://github.com/Benwellonedge28/Zenith"
categories = ["compilers", "development-tools", "command-line-utilities"]
keywords = ["compiler", "programming-language", "agi", "zenith", "zutc"]
homepage = "https://github.com/Benwellonedge28/Zenith"
license = "Apache-2.0 OR MIT"
readme = "README.md"

[[bin]]
name = "zenith"
path = "src/main.rs"

[[bin]]
name = "zenith-lsp"
path = "src/bin/zenith_lsp_main.rs"
required-features = ["lsp"]

[[bin]]
name = "zenith-pkg"
path = "src/bin/zenith_pkg_main.rs"
required-features = ["buildsystem"]

[lib]
name = "zenith_compiler"
path = "src/lib.rs"


[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[features]
default = []
# Lightweight, independently-buildable component features (see docker/ for
# per-component container images). These compile standalone without pulling
# in the rest of the aspirational `full` surface.
lsp = []
buildsystem = []
full = []
omniversal = []

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
anyhow = "1.0"
log = "0.4"
env_logger = "0.11"
toml = "1.1"

[dev-dependencies]
pretty_assertions = "1.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[profile.dev]
opt-level = 0
debug = true

[[bench]]
name = "compiler_bench"
harness = false
